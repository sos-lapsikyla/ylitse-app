import React from 'react';
import RN from 'react-native';
import { SafeAreaView } from 'react-navigation';

const MentorList = () => (
  <SafeAreaView>
    <RN.View>
      <RN.Text>MEET OUR MENTORS</RN.Text>
    </RN.View>
  </SafeAreaView>
);

export default MentorList;
